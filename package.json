{
  "name": "nodejs-patterns-fluent-interface-and-builder",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "node --experimental-json-modules src/",
    "test": "set NODE_OPTIONS=--experimental-vm-modules && npx jest --config jest.config.mjs --passWithNoTests --silent --noStackTrace --runInBand",
    "test:cov": "set NODE_OPTIONS=--experimental-vm-modules && jest --config jest.config.mjs --coverage --passWithNoTests --silent --noStackTrace --runInBand",
    "test:watch": "set NODE_OPTIONS=--experimental-vm-modules && jest --config jest.config.mjs --watchAll --passWithNoTests --silent --noStackTrace --runInBand"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "jest": "^26.6.3"
  }
}
